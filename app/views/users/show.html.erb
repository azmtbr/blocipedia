<div class="row">
  <div class="col-md-8">
    <div class="media">
      <br />
      <div class="media-body">
        <h2 class="media-heading"><%= @user.name %>'s profile</h2>
        <small>
          <%= pluralize(@user.wikis.count, 'wiki') %>
        </small>
      </div>
    </div>
  </div>
</div>

<h2>Wikis</h2>
<%= render @wikis %>
<br />

<%= link_to "Edit profile", edit_user_registration_path, :class => 'btn btn-default' %>
